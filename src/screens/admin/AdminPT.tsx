import React from 'react';
import Loader from 'components/loader';
import styles from './Admin.module.scss';

const AdminPT = ({ uid, uid_ }: typeAdminPT): JSX.Element => {
  return <></>;
};

interface typeAdminPT {
  uid?: string;
  uid_: string;
}

export default AdminPT;
