import React from 'react';
import { connect } from 'react-redux';

const mapStateToProps = () => {
  return {};
};

const mapDispatchToProps = () => {
  return {};
};

const Card = ({}: typeCard): JSX.Element => {
  return <></>;
};

interface typeCard {}

export default connect(mapStateToProps, mapDispatchToProps)(Card);
